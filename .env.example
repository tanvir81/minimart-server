# Example backend environment variables

PORT=5000
CORS_ORIGIN=http://localhost:3000   # change to your deployed frontend URL in production

CLERK_SECRET_KEY=sk_test_yourSecretKeyHere   # backend only, never expose in frontend

# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/minimart
# JWT_SECRET=your_jwt_secret_here
